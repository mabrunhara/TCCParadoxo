using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class animatorSapo : MonoBehaviour
{
    public Animator sapoAnimator;
    // Start is called before the first frame update
    void Start()
    {
        sapoAnimator = GetComponent<Animator>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
